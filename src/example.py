def example(dummy: int) -> int:
    """Dummy example to test repo setup

    Args:
        dummy (int): Variable for testing

    Returns:
        int: Same dummy, not modified
    """
    return dummy
